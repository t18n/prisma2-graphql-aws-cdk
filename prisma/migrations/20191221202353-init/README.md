# Migration `20191221202353-init`

This migration has been generated by Turbo at 12/21/2019, 8:23:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `prisma2_graphql_aws_cdk`.`User` (
  `birthday` datetime(3)   ,
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `email` varchar(191) NOT NULL DEFAULT '' ,
  `firstName` varchar(191)   ,
  `id` varchar(191) NOT NULL  ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `prisma2_graphql_aws_cdk`.`Order` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `currency` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `price` int NOT NULL DEFAULT 0 ,
  `status` boolean NOT NULL DEFAULT false ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `prisma2_graphql_aws_cdk`.`Product` (
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191) NOT NULL DEFAULT '' ,
  `serialNumber` varchar(191) NOT NULL DEFAULT '' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `prisma2_graphql_aws_cdk`.`Order` ADD COLUMN `user` varchar(191)  ,
ADD FOREIGN KEY (`user`) REFERENCES `prisma2_graphql_aws_cdk`.`User`(`id`) ON DELETE SET NULL;

ALTER TABLE `prisma2_graphql_aws_cdk`.`Product` ADD COLUMN `user` varchar(191)  ,
ADD FOREIGN KEY (`user`) REFERENCES `prisma2_graphql_aws_cdk`.`User`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.email` ON `prisma2_graphql_aws_cdk`.`User`(`email`)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20191221202353-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,39 @@
+datasource db {
+  provider = "mysql"
+  url      = env("DB_CONNECTION")
+}
+
+generator photon {
+  provider      = "photonjs"
+  binaryTargets = [env("BINARY_TARGET")]
+}
+
+model User {
+  id        String    @id @default(cuid())
+  email     String    @unique
+  birthday  DateTime?
+  firstName String?
+  products  Product[] @relation
+  orders    Order[]   @relation
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+}
+
+model Order {
+  id        String   @id @default(cuid())
+  price     Int
+  currency  String
+  status    Boolean
+  user      User?    @relation
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model Product {
+  id           String   @id @default(cuid())
+  name         String
+  serialNumber String
+  user         User?    @relation
+  createdAt    DateTime @default(now())
+  updatedAt    DateTime @updatedAt
+}
```


